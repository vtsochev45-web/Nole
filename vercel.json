{
  "buildCommand": "pip install markdown && python3 scripts/build-site.py",
  "outputDirectory": "_site",
  "framework": null,
  "env": {
    "SITE_BASE_PATH": ""
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
